# Test Python package configuration for integration tests
version: "1.0.0"
generated_at: "2025-01-25T10:00:00Z"

# Package manager settings
settings:
  package_manager: "pip"
  use_venv: false
  index_url: "https://pypi.org/simple"

# Version strategy
strategy:
  default: "compatible"

# Cleanup policy
cleanup_policy: "preserve"

# Test global packages (minimal set)
packages:
  # Development tool
  - name: black
    version: "~=23.0"

  # Testing tool
  - name: pytest
    version: ">=7.4.0"

  # CLI tool for testing
  - name: httpie
    version: "latest"

  # Utility
  - name: requests
    version: ">=2.31.0"

# No requirements files for testing
requirements_files: []
