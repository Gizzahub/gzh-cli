# gzh.yml - Organization-level configuration
# This file is created in each organization directory during synclone
# Path: <org-directory>/gzh.yml
# Generated by: gz synclone
# Version: 1.0.0

version: "1.0.0"
created_at: "2025-01-25T00:00:00Z"
last_synced: "2025-01-25T00:00:00Z"

# Organization metadata
organization:
  name: "ScriptonBasestar"
  provider: "github"
  url: "https://github.com/ScriptonBasestar"
  description: "Scripton Basestar's GitHub Organization"

# Sync configuration
sync:
  # Clone configuration used for this organization
  strategy: "reset"              # Strategy used: reset, pull, fetch
  protocol: "ssh"                # Protocol used: https, ssh
  branch: "main"                 # Default branch cloned
  depth: 0                       # Clone depth (0 = full history)
  bare: false                    # Whether bare repositories

  # Sync mode settings
  sync_mode:
    cleanup_orphans: true        # Remove directories not in repository list
    conflict_resolution: "remote-overwrite"  # Conflict handling strategy

# Repository manifest
repositories:
  - name: "project-alpha"
    path: "project-alpha"
    url: "git@github.com:ScriptonBasestar/project-alpha.git"
    branch: "main"
    last_commit: "abc123def456"
    last_synced: "2025-01-25T00:00:00Z"
    status: "active"

  - name: "project-beta"
    path: "project-beta"
    url: "git@github.com:ScriptonBasestar/project-beta.git"
    branch: "develop"
    last_commit: "def456ghi789"
    last_synced: "2025-01-25T00:00:00Z"
    status: "active"

  - name: "legacy-service"
    path: "legacy-service"
    url: "git@github.com:ScriptonBasestar/legacy-service.git"
    branch: "master"
    last_commit: "ghi789jkl012"
    last_synced: "2025-01-25T00:00:00Z"
    status: "archived"

# Excluded repositories (matched by ignore patterns)
excluded:
  - "test-repo-1"
  - "example-archive"
  - ".github"

# Applied filters during synclone
filters:
  visibility: "all"              # all, public, private
  include_pattern: ""            # Regex pattern for inclusion
  exclude_patterns:              # Patterns that were applied
    - "^test-.*"
    - ".*-archive$"
    - "^\\."

# Statistics
stats:
  total_repos: 25
  synced_repos: 22
  excluded_repos: 3
  failed_repos: 0
  total_size: "1.2GB"

# Authentication used (sanitized)
auth:
  method: "token"                # token, ssh
  token_var: "GITHUB_TOKEN"      # Environment variable used
  ssh_key: "~/.ssh/github_rsa"   # SSH key path used

# Custom metadata (user-defined)
metadata:
  team: "backend"
  project: "microservices"
  environment: "development"
  tags:
    - "golang"
    - "kubernetes"
    - "microservices"
