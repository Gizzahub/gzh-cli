# Suppress deprecation warnings for indirect dependencies
# These are mostly from react-scripts and will be resolved when upgrading to newer tooling
audit-level=moderate
fund=false
legacy-peer-deps=true